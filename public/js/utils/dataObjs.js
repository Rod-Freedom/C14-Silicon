export class UserObj {
    constructor (username, password) {
        this.username = username;
        this.password = password;
    }
}

export class Meths {
    static binaryLikes (num) {
        return num.toString(2);
    }
}